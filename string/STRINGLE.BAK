#include<stdio.h>
#include<conio.h>
void main()
{
char str[100];
int i,len=0;
clrscr();
printf("plese enter the string whose length you want:");
gets(str);
for(i=0;str[i]!='\0';++i)
	len++;
printf("length of te string is: %d", len);
getch();
}